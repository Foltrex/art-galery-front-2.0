import * as React from 'react';

interface IRepresentativesProps {
}

const Representatives: React.FunctionComponent<IRepresentativesProps> = (props) => {
  return (
    <div></div>
  );
};

export default Representatives;
